#pragma once
#ifndef ALL_H
#define ALL_H

#include "common.h"

// Your engine headers (make sure VC++ include dirs point to ILTD-Engine)
#include "framework.h"
#include "static_mesh.h"
#include "skinned_mesh.h"

// Only keep this if your engine headers rely on it in the global scope.
// (Better is: remove and use DirectX:: explicitly in cpp files.)
using namespace DirectX;

#endif // ALL_H
